#include <iostream>
#include <random>

using namespace std;


struct RandomDice
{
    /*
    static random_device seed_generator;
	static unsigned seed;
	static mt19937 mersenne_generator;
	static uniform_int_distribution<int> distribution;
	
    seed = seed_generator();
    mersenne_generator(RandomDice::seed);
    distribution(1,6);
     */
};
